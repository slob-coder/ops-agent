core:
  patrol_log: "Patrolling... [target={name}, mode={mode}]"
  emergency_stop: "🚨 Emergency stop activated: {reason}. Switched to read-only mode."
  llm_degraded: "🚨 LLM calls keep failing, switched to read-only mode.\nReason: {reason}\nI'll try auto-recovery every 5 min. Check API key / network."
  internal_error: "Encountered internal error: {error}, continuing."
  interrupted: "Current task interrupted ({error_type})"
  recovered: "⚠️ Detected unfinished work (incident={incident}), state recovered"
  onboard_done: "I've already onboarded. Loading existing notebook to continue."
  onboard_start: "First run, starting onboarding exploration..."
  onboard_analyzing: "Analyzing system environment..."
  onboard_complete: "Onboarding complete! Generated system-map.md and watchlist.md. Starting patrol."
  on_duty: "On duty, entering patrol mode."
  exit_signal: "Received exit signal, signing off."

incident:
  found: "[{target}] Anomaly detected (severity {severity}/10): {summary}"
  diagnose_rounds_exceeded: "Investigated {rounds} rounds without conclusion, attempting fix with current info"
  fix_attempts_failed: "⚠️ {attempts} fix attempts all failed, please check:\n{summary}"
  fix_plan_failed: "⚠️ Cannot create fix plan:\n{summary}"
  verify_failed: "Verification failed (attempt {attempt}/{max})"
  max_rounds_exceeded: "Max processing rounds ({rounds}) reached, issue still unresolved, pausing investigation."
  fix_all_failed: "⚠️ {attempts} fixes all failed verification:\n{summary}"
  enter_investigate: "🔍 Entering investigation mode"
  escalate_notify: "⚠️ Human escalation needed:\n{summary}\nDiagnosis: {hypothesis}\nReason: {facts}"
  escalate_complex: "⚠️ Complex issue, will continue trying to resolve:\n{summary}\nDiagnosis: {hypothesis}\nConfidence: {confidence}%\nIntervene anytime if needed."
  diagnose_complete: "Diagnosis complete: {hypothesis}"
  type_action: "🔧 Type: {rtype} | Starting autonomous fix"
  execute_plan: "About to execute: {action}"
  executing: "Executing..."
  exec_failed: "⚠️ Execution step failed, rolling back..."
  no_rollback: "No rollback steps available"
  rollback_executing: "Execution failed, re-diagnosing..."
  verify_passed: "✅ Verification passed, issue fixed!"
  verify_uncertain: "⚠️ Verification uncertain: no convergence during observation but no deterioration, recorded as partial fix"
  verify_fail_rediagnose: "Verification failed, re-diagnosing..."
  readonly_mode: "Read-only mode, not executing. Plan:\n{plan}"
  denied_by_trust: "Action denied by authorization rules: {action}"
  denied_by_limits: "⛔ Denied by limits engine: {reason}"
  human_rejected: "(Rejected by human)"
  plan_gen_failed: "Plan generation failed ({attempt}/{max})"

check:
  config_error: "❌ {count} config errors, please fix and retry"
  config_ok_warn: "✅ Config mostly complete, warnings should be addressed"
  config_ok: "✅ Config complete, ready to start"
  targets_format_error: "targets.yaml format error: {error}"
  limits_format_error: "limits.yaml format error: {error}"
  notifier_format_error: "notifier.yaml format error: {error}"
  llm_test_failed: "LLM connectivity test failed: {error}"

chat:
  operation_cancelled: "Operation cancelled (from {source})."

notifier:
  observe_failed: "Observe function failed 3 times: {error}"
  anomaly_recurred: "Anomaly recurrence detected: {signature}"
  observation_done: "Observed {duration}s, {checks} checks, no recurrence"

prompt:
  none: "(none)"
  readonly_yes: "Yes (read-only mode, no modifications)"
  readonly_no: "No"
  no_incident: "None"
  no_permissions: "(not configured, using default policy)"
  no_system_map: "(not yet explored, system topology unknown)"
  limits_disabled: "(limits engine disabled)"
  target_managing: "Currently managing target: **{name}** (type: {mode})"
  target_description: "Description: {description}"
  target_ssh: "Connection: SSH to {host}"
  target_ssh_hint: "Write shell commands directly, the Agent will execute via SSH on the remote host."
  target_docker: "Connection: Docker {host_info}"
  target_docker_hint: "Commands run on the local workstation. To manage containers use:"
  target_docker_ps: "  - `docker ps` / `docker logs <container> --tail 100`"
  target_docker_exec: "  - `docker exec <container> <command>` to execute inside a container"
  target_docker_restart: "  - `docker restart <container>` to restart a container"
  target_docker_inspect: "  - `docker inspect <container>` to view details"
  target_docker_compose: "  - Compose file available: `docker compose -f {compose_file} <command>`"
  target_k8s: "Connection: Kubernetes (context={context}, ns={namespace})"
  target_k8s_hint: "Commands run on the local workstation. To manage the cluster use:"
  target_k8s_get: "  - `kubectl get pods -n {namespace}` / `kubectl get all`"
  target_k8s_logs: "  - `kubectl logs <pod> -n {namespace} --tail=100`"
  target_k8s_describe: "  - `kubectl describe pod <pod> -n {namespace}`"
  target_k8s_exec: "  - `kubectl exec <pod> -n {namespace} -- <command>`"
  target_k8s_rollout: "  - `kubectl rollout restart deployment/<name> -n {namespace}` rolling restart"
  target_local: "Connection: Local workstation"
  target_others: "\nYou also manage other targets: {others}"
  target_others_hint: "(Targets rotate automatically each patrol round. If asked about other targets, you need to use the corresponding command prefix first)"
  target_source_repo: "\nSource code for this target:"
  target_source_repo_item: "  - {name}: {language}, path {path}"
  limits_actions: "Action quota: this hour {used_hour}/{max_hour}, today {used_day}"
  limits_incidents: "Concurrent Incidents: {active}/{max}"
  limits_tokens: "Token usage (this hour): {used}/{budget}"
  limits_cooldown: "⚠️ In failure cooldown, {remaining} seconds remaining"

observe:
  silence_known: "Known unresolved anomaly (silenced, {remaining}s remaining): [{target}] {summary}"
  all_normal: "All normal"
  monitor_wait: "Diagnosis suggests observation, re-checking in {seconds}s"
  collect_more_no_gaps: "LLM requested more info but provided no commands, attempting fix"
  collect_more_progress: "Collecting more info... (round {round})"

patch:
  weekly_scorecard: "📊 Weekly self-review {week} — Level: {level}  Fix rate: {rate:.0f}%"
